export VES_PREFIX=__VES_INSTALL_PREFIX_PLACEHOLDER__
export PATH=${VES_PREFIX}/bin:${PATH}
export LD_LIBRARY_PATH=${VES_PREFIX}/lib:${VES_PREFIX}/lib/vtk-5.10:${VES_PREFIX}/lib64:${LD_LIBRARY_PATH}
export OSG_FILE_PATH=${VES_PREFIX}/share/osgWorks/data:${VES_PREFIX}/share/osgBullet/data:${VES_PREFIX}/share/backdropFX/data:${VES_PREFIX}/share/latticefx/data
export OSGNOTIFYLEVEL=WARNING
export VJ_BASE_DIR=${VES_PREFIX}
export NO_RTRC_PLUGIN=TRUE
export NO_PERF_PLUGIN=TRUE

# if necessary, set PYTHONPATH
WXPYTHON_BASE_DIR=$(find ${VES_PREFIX} -type d -name "python*")

if [ -n "${WXPYTHON_BASE_DIR}" ]
then
    export PYTHONPATH=${WXPYTHON_BASE_DIR}/site-packages/wx-2.8-gtk2-unicode:${PYTHONPATH}
fi
