PACKAGE_NAME=sqrat
BASE_DIR="${DEV_BASE_DIR}/sqrat-0.8.3"

SOURCE_RETRIEVAL_METHOD=wget
SOURCE_FORMAT=tgz
SOURCE_URL=http://sourceforge.net/projects/scrat/files/Sqrat/Sqrat%200.8/sqrat-0.8.3.tar.gz

SOURCE_DIR="${BASE_DIR}"
BUILD_DIR="${BASE_DIR}"
INSTALL_DIR="${BASE_DIR}"
SKIP_PREBUILD="yes"

case $PLATFORM in
  Windows)
    unset BUILD_METHOD
    ;;
  Darwin | Linux)
    unset BUILD_METHOD
    ;;
esac
