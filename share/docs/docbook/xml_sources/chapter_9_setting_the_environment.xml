<?xml version="1.0" encoding="UTF-8"?>
<chapter>
  <title>Setting the Environment</title>

  <para>To specify the build/run environment, perform a one-time edit of the
  environment variables that are defined in the setup file, setup.bat on
  Windows and setup.tsh/setup.sh on UNIX/Linux platforms. If the paths of your
  environment variables contain spaces, you need to put the directory
  containing the spaces in quotation marks.</para>

  <para>Example:</para>

  <para><code> set VTK_HOME=C:/"Documents and
  Settings"/user/Desktop/vtk</code></para>

  <para><emphasis role="bold">Important variables for UNIX/Linux platforms
  only:</emphasis></para>

  <para><code>SCENE_GRAPH</code> Always set to OSG. Defines which scene graph to
  build for VE-Xplorer.</para>

  <para><code>TAO_BUILD</code> Always set to TRUE. In the future this flag can be
  used to support other CORBA ORBs as well.</para>

  <para><code>CLUSTER_APP</code> Set to TRUE if VE-Xplorer will be used on a
  graphics cluster.</para>

  <para><emphasis role="bold">Important variables include:</emphasis></para>

  <para><code> VE_SUITE_HOME</code> The location of the top level VE_Suite
  directory.</para>

  <para><code> VJ_BASE_DIR</code> The location of VR Juggler.</para>

  <para><code> VJ_DEPS_DIR</code> The location of VR Juggler
  dependencies.</para>

  <para><code> VTK_HOME</code> Visualization ToolKit (VTK) directory.</para>

  <para><code> WX_HOME</code> The wxWidgets install.</para>

  <para><code> XERCESCROOT</code> The location of the xerces build.</para>

  <para><code> WX_ROOT</code> The location of wxWidgets install (should be
  same as <code>WX_HOME</code>).</para>

  <para><code> OSGHOME</code> The location of OpenSceneGraph (if OSG version
  is built).</para>

  <para><code> ACE_HOME</code> The location of the ACE_wrappers
  directory.</para>

  <para><code> TAO_HOME</code> TAO location within ACE/TAO.</para>
</chapter>
