##Builds VE-Suite Libraries
#import os
#pj = os.path.join

Import( 'GetPlatform baseEnv ves_pkg' )

toolsSubdirs = Split("""
    builder
    vea
    aspen
""")

##Run SConscript files in all of those folders.
for d in toolsSubdirs:
   SConscript( dirs = d )
