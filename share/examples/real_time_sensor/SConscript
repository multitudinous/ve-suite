##
import os, sys, string
import SConsAddons.Util as sca_util
pj = os.path.join

Import( 'RootDir ves_pkg GetPlatform poco_options' )

fileBundle = ves_pkg.createFileBundle( pj( 'share', 'vesuite', 'examples', 'real_time_sensor' ) )
WarrantyToolDir = pj( RootDir, 'share', 'examples', 'real_time_sensor' )

#fileBundle.addFiles( [ 'warrantytool.ves' ] )
iveFile = sca_util.getFilesRecursiveByExt( WarrantyToolDir, [ '.ive' ] )
fileBundle.addFiles( iveFile )

WarrantyToolSubdirs = Split("""
    UserInterface
    GraphicalPlugin
""")

##Run SConscript files in all of these folders.
for d in WarrantyToolSubdirs:
   SConscript( dirs = d )
