import os, sys, string
pj = os.path.join

Import('baseEnv')
Import('Platform')

addNormals2Poly = Split("""
   addNormals2Poly.cpp
""")

appendVTK = Split("""
   appendVTK.cpp
""")

arrowCreator = Split("""
   arrowCreator.cpp
   cfdArrow.cpp
""")

combineFluentParticleFile = Split("""
   CombinePartFiles.cpp
""")

convertCellDataToPointData = Split("""
   convertCellDataToPointData.cpp
""")

convertSurfaceFileToStl = Split("""
   convertSurfaceFileToStl.cpp 
""")

convertVTK2Ascii = Split("""
   convertVTK2Ascii.cpp
""")

convertVTK2Binary = Split("""
   convertVTK2Binary.cpp
""")

createMiniFlowdata = Split("""
   createMiniFlowdata.cpp 
""")

makeVtkSurface = Split("""
   makeVtkSurface.cpp 
""")

mergeVertices = Split("""
   mergeVertices.cpp
""")

meshViewer = Split("""
   meshViewer.cpp
   cfdImage.cpp
""")

moveFieldToPointData = Split("""
   moveFieldToPointData.cpp
""")

removeVtkCellsOutsideBox = Split("""
   removeVtkCellsOutsideBox.cpp
""")

removeVtkPointData = Split("""
   removeVtkPointData.cpp
""")

scaleVTK = Split("""
   scaleVTK.cpp
""")

transformVTK = Split("""
   transformVTK.cpp 
""")

whatIsScalarRange = Split("""
   whatIsScalarRange.cpp
""")

env = baseEnv.Copy()
env.Append(CPPPATH = [os.getcwd()])
env.Append(CPPPATH = ['#/VE_Builder/Translator'])
env.Append(CPPPATH = ['#/VE_Builder/Preprocessor'])
env.Append(CPPPATH = ['#/VE_Builder/Utilities'])
env.Append(CPPPATH = ['#/VE_Xplorer'])
env.Append(LIBPATH = '/home/users/nelsonn/svn_Test/VE_Suite',
              LIBS = 'Shared_Library')
env.Program(pj('#','bin',Platform,'vr_addNormals2Poly'), source = addNormals2Poly)
env.Program(pj('#','bin',Platform,'vr_appendVTK'), source = appendVTK)
env.Program(pj('#','bin',Platform,'vr_arrowCreator'), source = arrowCreator)
env.Program(pj('#','bin',Platform,'vr_combineFluentParticleFile'), source = combineFluentParticleFile)
env.Program(pj('#','bin',Platform,'vr_convertCellDataToPointData'), source = convertCellDataToPointData)
env.Program(pj('#','bin',Platform,'vr_convertSurfaceFileToStl'), source = convertSurfaceFileToStl)
env.Program(pj('#','bin',Platform,'vr_convertVTK2Ascii'), source = convertVTK2Ascii)
env.Program(pj('#','bin',Platform,'vr_convertVTK2Binary'), source = convertVTK2Binary)
#env.Program(pj('#','bin',Platform,'vr_createMiniFlowdata'), source = createMiniFlowdata)
env.Program(pj('#','bin',Platform,'vr_makeVtkSurface'), source = makeVtkSurface)
env.Program(pj('#','bin',Platform,'vr_mergeVertices'), source = mergeVertices)
env.Program(pj('#','bin',Platform,'vr_meshViewer'), source = meshViewer)
env.Program(pj('#','bin',Platform,'vr_moveFieldToPointData'), source = moveFieldToPointData)
env.Program(pj('#','bin',Platform,'vr_removeVtkCellsOutsideBox'), source = removeVtkCellsOutsideBox)
env.Program(pj('#','bin',Platform,'vr_removeVtkPointData'), source = removeVtkPointData)
env.Program(pj('#','bin',Platform,'vr_scaleVTK'), source = scaleVTK)
env.Program(pj('#','bin',Platform,'vr_transformVTK'), source = transformVTK)
#env.Program(pj('#','bin',Platform,'vr_whatIsScalarRange'), source = whatIsScalarRange)
